{
  "name": "@gnars/gnars-frontend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "api:build": "turbo run build --filter=api"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.13.2"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "utf-8-validate": "^5.0.2"
  }
}
