{
  "name": "gnars",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "api:start": "yarn workspace api start"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
