{
  "name": "gnars",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "api:build": "turbo run build --filter=api"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.13.2"
  }
}