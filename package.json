{
  "name": "gnars",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "api:build": "turbo run build --filter=api"
  },
  "devDependencies": {
    "turbo": "^1.13.2"
  },
  "engines": {
    "node": "18"
  },
  "peerDependencies": {
    "utf-8-validate": "^5.0.2"
  }
}
